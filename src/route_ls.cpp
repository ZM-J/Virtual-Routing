#include "route_ls.h"

#include <string>

using namespace std;

int RouteLS::SendRouteMsg(string msg, string dest) {
    int errno = 0;
    return errno;
}

int RouteLS::RecvRouteMsg(string msgGet) {
    int errno = 0;
    return errno;
}

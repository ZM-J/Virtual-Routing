#include "route_algo.h"
#include "route_message.h"

int RouteAlgo::SendConnectionCheck() {
    // Check Connection
    // Can use same port as SendRouteMsg()?
    return 0;
}

int RouteAlgo::RecvConnectionCheck() {
    // Receive Connection
    // Can use same port as RecvRouteMsg()?
    return 0;
}